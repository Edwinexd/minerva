apiVersion: v1
kind: Service
metadata:
  name: minerva-embedder
  namespace: minerva
spec:
  type: ClusterIP
  selector:
    app: minerva-embedder
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      protocol: TCP
