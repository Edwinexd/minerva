apiVersion: v1
kind: Service
metadata:
  name: minerva-reranker
  namespace: minerva
spec:
  type: ClusterIP
  selector:
    app: minerva-reranker
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
      protocol: TCP
